#include<stdio.h>
int main()
{
    int a,b=7,c=8,d=10,e=6;//declare a variable
    scanf("%d",& a);// taking a input
    float avg =a+b+c+d+e;
    printf("average of 5 number:%.2f",avg);// if we put dot in b/w % and 2f we get less decimal place vice versa
    
}